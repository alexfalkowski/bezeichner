client:
  v1:
    host: localhost:12000
    timeout: 1s
    user_agent: "Bezeichner-client/1.0 gRPC/1.0"
    retry:
      timeout: 2s
      attempts: 3
debug:
  port: 6060
environment: production
telemetry:
  logger:
    enabled: true
    level: info
  tracer:
    enabled: true
    host: localhost:4318
    secure: false
